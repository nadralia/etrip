import React from 'react';
import { Text, View } from 'react-native';

const ReportsByCustomer = ({ params }) => (
	<View>
		<Text>ReportsByCustomer</Text>
	</View>
);

export default ReportsByCustomer;
